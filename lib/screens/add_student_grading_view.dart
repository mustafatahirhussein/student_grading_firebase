import 'package:flutter/material.dart';

class AddStudentGradingView extends StatelessWidget {
  const AddStudentGradingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
